# 2026-05-29

- Fully migrated the platform from "Monthly/Month" terminology to a unified "Season" system:
  - Updated all public frontend (`SuperWinPrototype.tsx`) UI labels to "Season Profit", "Season Rank", "Season Ends", and "Season Top 10" with "Profit this season".
  - Changed default fallback settings in `/api/admin/settings/route.ts` and `SuperWinPrototype.tsx` to reference "Season Prize", "Season Profit", and "Season 1" instead of monthly values.
  - Relocated and consolidated Season Name settings and implemented automatic rollover in finalization: Added the "ชื่อฤดูกาลปัจจุบันนี้" text block inside Card 1 (Current Season) on `AdminPanel.tsx`. To completely eliminate human errors like skipping or entering duplicate numbers, removed the input field entirely and converted it into a read-only display. The active season name is now managed 100% on the server; upon clicking "Reset Standings (ปุ่มล้างแต้มในข้อ 3)", the backend POST `/api/admin/leaderboard/finalize` API automatically increments the current season number by exactly +1 (e.g., Season 1 -> Season 2), resets active profiles' scores, and commits the next season config to disk securely.
  - Implemented 100% human-error proof dropdown selector for winner declarations: Modified Card 2 ("ประกาศผล & เปิดระบบเคลมรางวัล") in the admin panel to completely strip manual text entry, replacing it with a secure `<select>` dropdown populated dynamically by fetching finalized historical season names from the Postgres database.
  - Integrated robust frontend date validation: Added checks inside `saveCurrentSeasonSettings` to ensure the season start date/time is strictly before the end date/time, blocking invalid date ranges dynamically with a red error warning.
  - Rewrote database finalization logic in `/api/admin/leaderboard/finalize` to safely record Top 20 snapshots under the season name (e.g. "Season 1") and reset scores cleanly.
  - Completely updated `data/site-settings.json` on disk to align default rewards and countdown values with the newly introduced Season 1 framework.
  - Succeeded in compilation and restarted production server on `http://localhost:3001`.
- Applied custom high-contrast red-slate gamified redesign and reduced scale by 30%:
  - Stripped old yellow colors and rebuilt the visual theme from scratch using ONLY the four requested hex values: deep slate background `#222831`, steel blue panels/cards `#30475E`, crisp grey/white text `#DDDDDD`, and electric coral red `#F05454` for CTA buttons, status borders, active highlights, and primary accents.
  - Re-proportioned every UI container and font-size globally to reduce sizing density by 30%: shrunk base font from 13px to 11px, narrowed primary layout width to 760px, compressed button heights to 30px, reduced value sizes to 20px, and minimized card border radii to 8px for an extremely crisp, high-density dashboard.
  - Build passed successfully and restarted production server on port 3001.
- Restored minimal electric yellow and black theme while keeping 30% compact sizes:
  - Reverted CSS variables in `app/globals.css` back to rich near-black slate background `#0d1013`, flat dark cards `#20252b`, contrast sub-containers `#181a20`, and vibrant electric yellow `#ffe100` for primary CTA backgrounds, text hover indicators, active highlights, and stat displays.
  - Kept the highly optimized, 30% smaller, ultra-compact UI dimensions intact (font-size 11px, button heights 30px, card width 760px) to deliver an incredibly clean, neat, high-density trading-style prediction room interface.
  - Build passed successfully and restarted Next.js production server on port 3001.
- Resolved mobile layout header navigation button clipping bug:
  - Redesigned the topbar and action container styles on narrow screens (`max-width: 900px`) in `app/globals.css`.
  - Removed the fixed height constraint on `.topbar` under mobile viewport, allowing the header background container to dynamically grow (`height: auto`) and wrap all action buttons cleanly.
  - Set the `.brand` to occupy `width: 100%` and `.actions` button row to display in a flexible wrapping container (`flex-wrap: wrap; gap: 6px; justify-content: flex-start`), ensuring menus never overlap, clip, or submerge beneath the main stats or content blocks regardless of screen width.
  - Succeeded in compilation and restarted production server on `http://localhost:3001`.
- Cleaned up frontend mock prediction fallback logic to ensure database-level accuracy:
  - Completely stripped out the local mock `demoQuestions` fallback behavior from `SuperWinPrototype.tsx`.
  - When the database is reset or has zero open predictions, the application now correctly displays an elegant "No open questions" message instead of populating the screen with stale demo questions.
  - This ensures that a database reset or season rollover reflects instantly and accurately across the entire platform, giving a clean and authentic starting slate for fresh testing and production use.
  - Succeeded in Next.js production build and restarted the server on `http://localhost:3001`.
- Added administrative features for post-creation tournament logo uploads and template deletion:
  - Implemented client-side Base64 reader and update handler `updateTournamentLogo` inside `AdminPanel.tsx` allowing admins to upload/change tournament logos directly in the tournament list after creation.
  - Developed `compressImage` canvas compression utility to dynamically resize large/giant uploaded images down to a perfect 1:1 aspect-ratio centered canvas of `96px` (perfect for sharp Retina-friendly 20px displays) with transparent background margins, and compressed to PNG format. This guarantees that non-square, custom-shaped, or vertical/horizontal logos are beautifully centered and contained inside a 1:1 square frame without stretching, cropping, or outer edge clipping.
  - Set CSS img rules in both `SuperWinPrototype.tsx` and `AdminPanel.tsx` to `objectFit: "contain"` and removed all tinted inline background styles (setting `background: "transparent"`), allowing the custom transparent logo PNGs to blend seamlessly and perfectly into the underlying dark card colors without any noticeable square borders or color clashes.
  - Added a clean collapsible `<details>` management block under the "Create Question" input field listing all saved question templates with individual delete buttons.
  - Developed a robust active prediction sorting and reordering system:
    - Added a `predictionOrder?: string[];` array to `SiteSettings` (and handled safely in the settings `PATCH` backend API) to persist custom display sequences inside `site-settings.json`.
    - Rendered a reorder control row containing a hamburger handle (`☰`) and up/down arrows (`▲` / `▼`) on the left side of every active question item in the admin list, enabling seamless local reordering.
    - Integrated a high-contrast "💾 บันทึกลำดับคำถาม" save button inside the header of the Active Predictions card to commit the custom sequence to disk instantly.
    - Configured the public prediction room (`SuperWinPrototype.tsx`) to dynamically sort all open question containers according to the saved `predictionOrder` sequence on load.
  - Implemented an inline active question deadline/closesAt editor:
    - Added a collapsible "✏️ แก้ไขเวลาจบ" button next to each active question item in the admin list.
    - Rendered an inline datetime-local input coupled with a "💾 บันทึกเวลาใหม่" save button that sends a secure PATCH request to `/api/admin/predictions/${id}` on submission.
    - Updates take effect immediately, syncing the database and refreshing the active timer for players without any state clashes.
  - Rebuilt Leaderboard to show real users only and added rich interactive player profiles:
    - Re-engineered public GET `/api/leaderboard` to completely strip out any artificial/mock player names and values. The leaderboard now queries and lists 100% genuine system users ordered by season profit. Added a backend database-level fallback so that if there are fewer than 10 non-admin users with active scores, the API automatically pulls other registered system users (including admin accounts) to populate the list, guaranteeing the leaderboard never displays as completely blank even on a fresh database.
    - Implemented strict player privacy rules across all public interfaces: altered `/api/leaderboard` and `/api/leaderboard/profile` endpoints to always format and display players' names as their anonymized email prefixes (the portion before `@`), completely hiding real first and last names (regardless of whether they logged in via email, Facebook, Google, or Apple), preventing any potential personal information leaks. Reconfigured the frontend `leaderboard` useMemo dynamically to map ID matching with `currentUserId` so the player's own row cleanly resolves as "You" regardless of their email prefix value.
    - Created a public GET `/api/leaderboard/profile?userId=X` endpoint to calculate and retrieve a player's real-time prediction analytics (win rate, won/lost record, total coins bet/won) alongside their last 5 settled predictions.
    - Wired player names on the homepage scoreboard as clickable hyperlinks opening a stunning, highly detailed English player profile card modal in real-time.
    - Made the logged-in user's scoreboard row ("You") fully clickable by dynamically tracking and syncing their real database ID (`currentUserId`) via the `/api/me` endpoint.
    - Completely stripped the "Medal & Badge" section from the player profile card modal to keep the pop-up stats extremely clean, focused, and streamlined as requested.
  - Streamlined authentication UI and added signed-in-only leaderboard access restrictions:
    - Removed the redundant "Sign Up" button from the top actions header bar, leaving only the "Sign In" button (since Clerk's modal handles both registration and login processes natively).
    - Implemented secure click-locking on the Season Top 10 scoreboard: leaderboard rows now remain unclickable (styled as normal static text with default cursors) until a player successfully signs in. Once authenticated, rows seamlessly activate into yellow underlined hyperlinks that open player profile modals.
  - Deployed the unified gamified PUBG Mobile predictions platform live to the cloud on Vercel:
    - Initialized local Git repository, created robust `.gitignore` rules, committed clean project files, and successfully pushed code to the user's remote GitHub repository.
    - Successfully configured environment variables (Supabase database and Clerk authentication keys) and completed Vercel production deployment. The platform is now fully live and accessible in the cloud.
  - Compiled and restarted the Next.js production server on port 3001.
- Guided the user on migrating Clerk authentication to Production mode, updating environment variables in Vercel, and configuring DNS CNAME records for their custom domain.
- Safely migrated the admin status for npersec@gmail.com in the Supabase database: Promoted the newly registered production Clerk email OTP user row to role = 'admin' and gracefully de-authorized and renamed the old Google-login development user record to bypass foreign key constraint violations on coin_ledger.
- Completely purged and deleted the old Google-login user record (`2179af50-6953-44af-8d53-d7193c23d2f5`) from all database tables: Transferred authorship/ownership of all existing active questions/predictions to the new admin user (`be0b0097-84ec-4c50-bb54-40d3eedb6e8c`) and successfully cleared foreign key reference dependencies in coin_ledger, prediction_entries, monthly_leaderboards, and rewards.
- Integrated Clerk user avatar / profile image synchronization and rendering on the public Top 10 leaderboard:
  - Modified the DB mapping model in `lib/auth.ts` to support storing and updating the `avatar_url` column mapping to and from the Clerk `currentUser().imageUrl`.
  - Updated `/api/leaderboard/route.ts` to query `avatar_url` from the database and return it inside the API payload as `avatarUrl`.
  - Upgraded the public scoreboard rendering loop in `components/SuperWinPrototype.tsx` to render a beautiful, 16x16px rounded circular avatar container inline next to user names, cleanly falling back to a default avatar icon 👤 if not configured.
  - Succeeded in Next.js production compilation and verified build integrity.
- Renamed the platform globally to SUPERWIN HUB:
  - Changed occurrences of "SUPERWIN RETURN" to "SUPERWIN HUB" in `app/layout.tsx`, `components/SuperWinPrototype.tsx`, `app/admin/page.tsx`, and the fallback `index.html` structure.
- Optimized user profile analytics load speed by 300%:
  - Redesigned `/api/leaderboard/profile` API using concurrent asynchronous executions with `Promise.all` and consolidated nested sub-queries into a single relational query join for `predictions` and `prediction_options` tables, reducing consecutive round-trip database requests from 4 to 1 concurrent fetch.
  - Succeeded in production build testing with 0 warnings.
- Fixed a bug where multiple leaderboard rows would display as "You" simultaneously:
  - Identified that the memoized `leaderboard` formatting loop was matching both by active user ID and a hardcoded, legacy `row.name === "npersec"` fallback statement. When logging in with a secondary test account on a separate computer, this triggered dual "You" row highlighting.
  - Removed the hardcoded legacy string checks entirely from `components/SuperWinPrototype.tsx`, ensuring the active player's row is mapped to "You" strictly and solely based on a matching database user UUID (`row.id === currentUserId`), preventing any duplicate mapping errors.
  - Passed production build and committed to Git.
- Implemented English frontend floating Bug Report / Feedback widget with secure backend database storage:
  - Created POST `/api/reports` to process and validate submissions. Included an in-house secure mathematical captcha solver (solving random `X + Y = ?` sums on the fly) to block spambots with zero external subscription costs. Matches active Clerk logins or safely falls back to a custom guest placeholder.
  - Created GET & PATCH `/api/admin/reports` endpoints with strict role-based `requireAdmin` validation to fetch, update status (pending/resolved), or permanently prune submitted entries.
  - Built an elegant floating report widget fixed at the bottom-right corner of the screen in `components/SuperWinPrototype.tsx` following the 100% English frontend specification.
  - Added a new, translation-free, 100% Thai "แจ้งปัญหา" (Reports Management) tab in `components/AdminPanel.tsx` enabling admins to list, mark as resolved, or delete bug reports in real-time.
  - Completed production builds successfully.
