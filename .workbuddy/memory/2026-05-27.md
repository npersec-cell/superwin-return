# 2026-05-27

- Verified Supabase connectivity after user ran `supabase/grants.sql`: Next.js dev server started successfully and `/api/health/supabase` returned HTTP 200, confirming service_role access and schema availability.
- Started Clerk integration for SUPERWIN RETURN: authenticated Clerk CLI, initialized project with Clerk app `app_3EGE6paImpN0lUebsO3SA0C6QmO`, added ClerkProvider/sign-in/sign-up routes/proxy, adjusted proxy matcher to include `/__clerk/(.*)` and keep `/` public, replaced demo Login with Clerk Sign In/Sign Up/UserButton controls, and confirmed `npm run build` passes. Clerk doctor showed logged-in/linked/app reachable, with sandbox warnings about host-only state. Preview worked on `http://localhost:3001`.
- Fixed Clerk signup/loading issue by adding `/__clerk/(.*)` to public route matching in `proxy.ts` so Clerk internal proxy routes are not protected by auth middleware. Rebuilt successfully and restarted preview at `http://localhost:3001`.
- Implemented Phase 2.1 user sync: replaced placeholder `lib/auth.ts` with Clerk `auth()`/`currentUser()` and Supabase get-or-create user logic, updated `/api/me` to return real signed-in user data, and updated frontend account state to fetch `/api/me` after Clerk sign-in and show Synced/Syncing/Sync Error status. Build passed and preview restarted at `http://localhost:3001`.
- Implemented Phase 2.2 real claim/history: `/api/claim` now requires Clerk user, checks claim cooldown, updates Supabase `users.coin_balance`, and writes `coin_ledger`; added `/api/history` with filter/pagination from `coin_ledger`; frontend signed-in users now call real claim/history APIs while signed-out users keep demo behavior. `npm run build` passed and preview restarted at `http://localhost:3001`.
- Fixed user-facing issues after Phase 2.2: signed-out view now only shows Sign In/Sign Up and a login-required panel, signed-in view shows app menus; prediction amount input changed to fixed casino-style buttons 5/10/50/100/500 plus Clear; signed-in History filtering now reloads filtered rows from `/api/history` instead of showing unfiltered Claim rows. Build passed and preview restarted at `http://localhost:3001`.
- Revised signed-out SUPERWIN RETURN view to show public open questions, Monthly Top 10, and Prize panels while only Sign In/Sign Up controls remain in the header. Fixed History modal display to client-filter current rows so tabs do not show mismatched actions while server filtering loads. Added exact question close time in Bangkok timezone (`UTC+7`) beside each close countdown. Build passed and dev server was restarted with a longer timeout on `http://localhost:3001`.
- Enhanced Coin History detail display: demo Predict/Payout rows now include tournament, question, selected answer, approximate return percentage, status/result, and amount; history details render as stacked detail lines for better gambling-site-style record completeness. Build passed and preview restarted at `http://localhost:3001`.
- Implemented Phase 3 real prediction foundation: added `GET /api/predictions/open`, `GET /api/predictions/running`, and `POST /api/predictions/predict`; seeded demo predictions/options into Supabase via `scripts/seed-predictions.mjs`; frontend now loads open questions from Supabase for public/signed-in users, signed-in predictions deduct coins, create `prediction_entries`, write detailed `coin_ledger` rows, and refresh Running/History from backend. Build passed and preview remains available at `http://localhost:3001`.
- Fixed missing questions after Phase 3: updated `scripts/seed-predictions.mjs` to refresh existing demo predictions' `opens_at`, `closes_at`, status and fee on rerun; reran seed script; added frontend fallback to reset stale localStorage deadlines and show demo questions if API returns no rows; restarted stuck dev server. `/api/predictions/open` now returns 4 rows.
- Implemented Phase 4 Admin Panel: added `requireAdmin()` in `lib/auth.ts`, local `scripts/make-admin.mjs`, admin APIs for listing/creating/updating predictions, `/admin` page with `AdminPanel`, and an Admin link for admin users. Promoted the current test user to admin, `npm run build` passed, and preview opened at `http://localhost:3001/admin`.
- Implemented Phase 5 result settlement: added admin resolve/refund endpoints for predictions, including win/loss/refund entry status updates, payout/refund ledger rows, user coin balance changes, and monthly/lifetime profit deltas. Updated Admin Panel with winning-option selector plus Resolve and Cancel + Refund buttons. `npm run build` passed and preview opened at `http://localhost:3001/admin`.
- Refined Admin Panel UX after feedback: separated admin sections into `New Question`, `Running Now`, and `All Questions`; live/open questions now have immediate `Close Now`, Resolve, and Cancel + Refund controls; answer creation changed from newline textarea to one-by-one add/remove controls. Started a detached Next.js dev server on port 3001 with logs in `dev-server.out.log` / `dev-server.err.log` so the site stays available longer than normal tool background tasks.
- Clarified Admin Panel controls and fixed responsive clipping: header now shows `role: admin`, New Question includes a status guide for draft/open/closed/canceled, Running Now includes action explanations, and CSS was updated so admin pages use auto height, visible overflow, wrapping buttons, and scrollable page layout instead of clipping bottom controls. Build passed and `/admin` preview reopened on port 3001.
- Further simplified Admin Panel: removed status dropdown/guide from New Question and defaults new questions to `open`; added `Admin Emails` panel plus `POST /api/admin/users/make-admin` to promote signed-up users by email; grouped front-page questions by tournament so each tournament name appears once as a section header. Build passed and detached server restarted on port 3001.
- Updated Admin Panel after feedback: added `GET /api/admin/users` and an admin-only list showing current admin emails only; translated AdminPanel UI/messages/buttons into Thai; added optimistic local updates and clearer feedback for status changes so Close Now visibly moves items out of Running Now; kept build passing and restarted port 3001.
- Fixed admin action JSON parsing by adding a safe `requestJson` helper for AdminPanel fetches so HTML/auth error responses show readable messages instead of `Unexpected token '<'`. Added `POST /api/admin/users/remove-admin` plus UI remove buttons for admin users other than the current admin. Added file-backed `site-settings.json` with public/admin settings APIs, AdminPanel editing for Info text and Reward fields/image, and frontend display of editable Info and square reward image. Build passed and port 3001 restarted.
- Fixed admin API redirect issue causing `เซสชันหมดอายุหรือ API ไม่ถูกต้อง`: added `/api/admin(.*)` to public route matcher in `proxy.ts` so admin route handlers handle auth and return JSON 401/403 instead of Clerk HTML redirects. Verified `/api/admin/users` now returns JSON unauthorized when signed out. Build passed and port 3001 restarted.
- Broadened API middleware handling after admin still received HTML redirects: changed `proxy.ts` to mark all `/api(.*)` paths public at middleware level so each route handler owns auth and returns JSON instead of Clerk redirect HTML. Rebuilt, fully stopped port 3001 processes, restarted detached server, and verified `/api/admin/users` returns JSON 401 instead of HTML when signed out.
- User still saw stale admin API error because an old detached Node dev server (PID 32508) was still serving port 3001. Killed the old process with `taskkill`, restarted a fresh server (PID 19704), and verified `/api/admin/users` and `/api/admin/settings` both return JSON 401 when signed out rather than HTML.
- Finalized Clerk proxy/API handling: excluding APIs from middleware made `auth()` fail, so reverted to matching API routes in `proxy.ts` while marking `/api(.*)` as public to supply Clerk auth context without `auth.protect()` redirects. Killed stale PID 11092, rebuilt from clean `.next`, restarted fresh server PID 35096, and verified `/api/admin/users` returns JSON 401 while `/api/settings` returns JSON 200.
- Dynamic admin route handlers like `/api/admin/predictions/[id]/refund` still returned HTML 500 in Next dev due to a Turbopack/Jest worker runtime crash. Switched runtime serving from `next dev` to `next start` using the existing production build on port 3001. Verified the refund endpoint now returns JSON 401 when signed out rather than HTML 500.
