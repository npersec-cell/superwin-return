# 2026-05-26

- Discussed concept for a low-cost PUBG MOBILE match prediction web app: Clerk-based login, user email/UID storage, hourly coin claim with anti-cheat controls, admin-configured prediction markets, pari-mutuel payout math with system fee/burn, and leaderboard considerations. No HTML or code was created.
- Refined prediction app rules: no real-money cash-out, monthly rewards for coin leaders, unlimited per-question betting, users can bet multiple times across options, canceled questions refund 100%, admin can set start/end dates, frontend in English and admin in Thai. Recommended separating coin balance, monthly leaderboard score, and lifetime stats instead of deleting only the winner's full coin balance.
- Outlined the first-build system scope for the PUBG MOBILE prediction app: public frontend pages, user auth/profile/coin claim/betting flows, admin panel, backend APIs, database/ledger, security/rate-limit/audit, and low-cost infrastructure choices.
- Checked official pricing pages for suggested low-cost stack: Clerk, Vercel, Supabase, and Upstash have free tiers suitable for early MVP; likely first paid bottleneck is database/production reliability or hosting usage, not authentication.
- User noted the PUBG MOBILE prediction app scope feels large. Recommended cutting the first version to a very small MVP: login, claim coins, list questions, bet, admin create/resolve question, leaderboard, and transaction ledger only.
- Clarified product scope: the app should be pure prediction only; tournament is only a label/name for grouping questions, not a full tournament module. Suggested treating tournament_name as a simple field or lightweight grouping entity.
- Locked MVP decisions for SUPERWIN RETURN: use recommended stack (Next.js, Clerk, Supabase/Neon, Vercel), claim 100 coins per hour, 3% pool fee, monthly leaderboard based only on profit from day 1 to month end, coin balance not used for ranking, and show top 10.
- Reward model update: leaderboard shows top 10, but only selected rank(s) receive rewards. Initial reward idea is a PUBG MOBILE in-game top-up for the winner, with one public proof image/receipt slot on the website after fulfillment. Recommended adding reward status and proof image fields while avoiding exposing private user/payment details.
- Created `index.html` static MVP prototype for SUPERWIN RETURN using a strict black/yellow/white minimal PUBG MOBILE style. Includes demo login, coin claim cooldown, prediction betting cards, Top 10 leaderboard, reward proof slot, and admin mock. No backend implemented yet.
- Revised `index.html` to UI-only and more minimal, inspired by Polymarket's compact market layout: removed heavy hero/admin sections, reduced copy, added compact market list, side bet panel, account summary, Top 10, and reward proof row while keeping black/yellow/white only.
- Further redesigned `index.html` after user feedback: centered the entire app in a narrow mobile-first column, removed crowded full-width layout, reduced menu count, softened harsh square frames with rounded cards, and changed options into compact betting-style rows suitable for many team choices.
- Updated `index.html` again based on UI feedback: improved font stack and smaller typography, structured the design for multiple prediction questions, added scrollable compact option lists per question to support many answers, and reduced oversized text while keeping the centered minimal black/yellow/white style.
- Changed `index.html` font to Space Grotesk across the whole SUPERWIN RETURN UI using Google Fonts with system fallback.
- Updated `index.html` based on user feedback: added small SuperWin logo asset, changed palette from black/yellow/white to logo-inspired dark/red/gold/cream, removed page scrolling, changed each question to show a dropdown answer selector, replaced "bet" wording with "Predict"/coins language, added 100/500/1000 quick coin amount buttons, and changed Reward stat to Monthly Rank context.
- Refined `index.html` UX after screenshot feedback: main question panel title now uses tournament name (`Super League`) instead of "Open Questions", removed "Answer by dropdown", separated Claim 100 into the top bar so it no longer overlaps prediction controls, added a clear prize display (`Razer Card`, `10,000 UC`, Rank 1 by Monthly Profit), and added a concise rule line for coin selection and prediction flow.
- Further clarified `index.html` per-question flow: removed shared coin amount/rule area, each question now has its own dropdown, quick coin buttons (100/500/1000), coin input, and Predict button to reduce misclick risk. Added a History control and Coin History panel showing claims, prediction spending, and payouts. Renamed leaderboard panel to `Monthly Top 10`.
- Updated `index.html` again: Coin History is no longer shown in the right sidebar and now opens from the History button as a modal with lifetime/since-account-creation records grouped by month and a summary. Question rows are compact by default; coin shortcuts, coin input, and Predict only appear after selecting a specific question.
- Refined `index.html` after more UX feedback: replaced native select with a custom dropdown to avoid flicker, removed pool display, added approximate return percentage per answer option, changed quick coin buttons to 10/20/50/100 and made repeated clicks cumulative, and expanded History modal with summary plus detailed date/time/action/detail/amount records grouped by month.
- Added a top `Running` button/menu to `index.html`. After a user clicks Predict, the prediction is saved to a running list, the top button count updates, the question row indicates running status, and a modal shows all currently running predictions with question, selected answer, coins, and approximate return.
- Updated `index.html` based on user feedback: Running modal now only lists active running predictions and no explanatory text; question row meta text was simplified to avoid confusing "approx/running" descriptions. History modal now removes the summary/explanation and shows only detailed rows with pagination (10 rows per page, max 500 retained records/50 pages; older records discarded).
- Added History filters to `index.html` for All, Predict, Claim, and Payout actions, with pagination applying to the selected filter. Replaced the Prize stat with a live Month Ends countdown showing days/hours/minutes remaining in the current monthly leaderboard cycle.
- Updated `index.html` to use General Sans across the site via Fontshare. Added an Info menu/modal with How to Play, How to Win Reward, and a Google Translate widget for multiple languages.
- Switched `index.html` font stack to Untitled Sans (with system fallback) after user found General Sans too heavy. Expanded Info modal content with backend/system details discussed so far: Clerk login/email/UID, hourly claim, per-question running prediction flow, approximate returns, 3% fee, cancel refunds, admin setup, server-side ledger, and monthly leaderboard separation.
- Simplified `index.html` Info modal to user-facing content only: How to Play, Reward, Question Time, and Language. Removed System Rules, History and Tracking, Admin and Backend, specific prize name/value, and the monthly profit formula. Added per-question closing countdowns; open questions disappear when expired while submitted predictions remain in Running waiting for result.
- Reviewed the current SUPERWIN RETURN frontend prototype for missing user-facing elements. Concluded it is mostly complete for UI validation; remaining frontend items before backend are clear empty states, mobile validation, reset/demo-state control, and final copy consistency.
- Polished `index.html` user-facing prototype: added a Reset Demo button to clear localStorage test data, changed to a universal Helvetica/Arial/system font stack close to Untitled Sans so all users can see a similar font without licensed files, improved mobile behavior by allowing vertical scroll and showing side panels on small screens, added a Running empty state, and cleaned copy consistency.
- Added subtle interaction transitions to `index.html`: buttons now have light hover/active transitions, dropdown and modal openings use a short soft animation, question row borders transition gently, and reduced-motion preferences are respected.
- Created `SUPERWIN_RETURN_SYSTEM_SPEC.md`, a system specification for SUPERWIN RETURN covering goals, stack, user/admin flows, prediction rules, payout logic, leaderboard/reward rules, database schema, backend APIs, security/anti-cheat rules, build phases, and current frontend prototype status.
- Implemented SUPERWIN RETURN Phase 1 foundation as a Next.js/TypeScript project: package setup, app router files, migrated React client prototype, public logo asset, placeholder auth/db utilities and API routes, Supabase schema draft, environment template, and project archive. `npm install` succeeded with a Node engine warning; `npm run build` passed after TypeScript fixes. Dev server preview ran at `http://127.0.0.1:3000`.
- Updated `supabase/schema.sql` to make `predictions_winning_option_fk` idempotent after Supabase returned error 42710 because the constraint already existed on rerun.
- Added Supabase `.env.local` configuration from user-provided values and wired `lib/db.ts` plus `/api/health/supabase`. Build passed, but health check returned `permission denied for table users`, indicating table privileges need to be granted to service_role. Created `supabase/grants.sql` for the user to run in Supabase SQL Editor.
