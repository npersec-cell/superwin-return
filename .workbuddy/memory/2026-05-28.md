# 2026-05-28

- Redesigned SUPERWIN RETURN UI using the user's Miro-inspired design reference while preserving black/yellow as the core brand palette. Updated `app/globals.css` with dark/yellow + pastel tokens, pill buttons, rounded cards, board/sticky-note style question cards, responsive no-clipping layout, and matching admin styles. Adjusted frontend labels to `Visual Prediction Board` / `Open Boards`, kept Thai admin labels, ran `npm run build` successfully, and started production server on `http://localhost:3001`.
- Reworked the UI again toward a Binance-inspired dark trading-platform style after user rejected the Miro direction. Kept existing menus/flows, shifted colors away from exact Binance values (`#0d1013`, `#FAD733`, `#20252b`), removed pastel/gradient sticky-note styling, restored labels to `Prediction Room` and `Open Tournaments`, tightened radii/card surfaces, and changed dropdowns to opacity/transform/visibility transitions for smoother open/close. Build passed and production server restarted on port 3001.
- Applied screenshot-based UI fixes: brightened yellow to `#ffe100`, centered button/pill text vertically with fixed 40px control height, reduced app max width to 920px for more side breathing room/mobile friendliness, improved dropdown height/scrollbar styling and option touch targets, and made leaderboard content scroll within the panel so all 10 ranks can be reached. Build passed and production server restarted on port 3001.
- Fixed remaining clipping from screenshots by removing fixed app height, making main panels/questions/leaderboard overflow visible, switching page height to auto with page-level scrolling, increasing dropdown z-index, and changing side panel rows to auto so Monthly Top 10 can render all ranks without being cut by the Prize card. Build passed and production server restarted on port 3001.
- Adjusted header/overall density per screenshot: reduced top page padding to 6px, increased responsive side gutters while narrowing main app max width to 820px, reduced base font to 12px, shrank header logo/title/meta text, tightened topbar padding/gaps, reduced controls from 40px to 34px height, and narrowed mobile app width to 390px. Build passed and production server restarted on port 3001.
- Updated Running modal details: removed the confusing `approx. returns %` from the modal rows as requested to avoid pool calculation confusion, and added the actual prediction Bangkok date/time (`th-TH` string format) sourced from `createdAt` for complete detailed records. Build passed and production server restarted on port 3001.
- Restored 100% English convention on public frontend: translated Bangkok time label and text prefix in `RunningModal` into English (`Predict time: ...` in `en-GB` format). Sourced project guidelines and saved them to `.workbuddy/memory/MEMORY.md` to permanently record that public pages are strictly English while admin pages are strictly Thai. Build passed and production server restarted on port 3001.
- Removed Proof row from Prize card: removed the `Proof: After Completed` layout row from `SuperWinPrototype` and stripped the corresponding editable input from `AdminPanel` settings. Build passed and production server restarted on port 3001.
- Added UTC+0 target ending date under Month Ends countdown: calculated the exact UTC datetime for the month transition (e.g. `31 May 2026 17:00 UTC+0` which maps to 1 June Bangkok 00:00 AM) and displayed it in a small meta label right beneath the Month Ends countdown stats block. Build passed and production server restarted on port 3001.
- Cleaned up Info modal and added admin input labels: removed the redundant `Language` block from the `InfoModal` public popup. Added precise yellow Thai headings (e.g. `How to Play (วิธีเล่น)`) above each of the three textareas inside the `AdminPanel` site settings card so operators know exactly what each box controls. Build passed and production server restarted on port 3001.
- Implemented client-side caching for Coin History to prevent lagging/flickering tabs: added a state-backed `historyCache` inside `SuperWinPrototype` keyed by `filter-page`; switching tabs or pages now renders cached records instantly without fetching unless a new prediction or claim action triggers a `forceRefetch` to sync the state. Build passed and production server restarted on port 3001.
- Updated Prize card and admin settings: removed the file uploader completely and replaced it with a Month input text block in `AdminPanel` settings. Enabled dynamic display of this Month text (e.g. `May 2026`) in the public frontend Prize card. Build passed and production server restarted on port 3001.
- Refactored AdminPanel layout and Settings API: split the unified site settings card into separate `ข้อความ Info` and `ข้อมูลรางวัล` forms, each with its own independent save button. Replaced the manual Month string input with Month and Year dropdown lists. Created `/api/admin/leaderboard` to fetch the top 10 real users based on monthly profit, dynamically populating the `Winner (ผู้ชนะ)` select dropdown. Standardized the admin panel style to a clean, single-column vertical card stack (max-width 600px, centered) to keep the layout extremely clean, neat, and highly readable. Build passed and production server restarted on port 3001.
- Resolved long question text truncation: removed `white-space: nowrap` and `text-overflow: ellipsis` constraint from `.question-title`, set layout alignment of `.question-top` to `align-items: start`, added `word-break: break-word` and line-height controls to prevent overlapping, ensuring lengthy tournament questions wrap gracefully to multiple lines without being clipped in the viewport. Build passed and production server restarted on port 3001.
- Restructured question card layout for full-width title display: changed `.question-top` to a vertical block display, placing the question title at the top of the card spanning 100% of the available width. Moved the Closes-In meta text and the answer select dropdown to a symmetric second row underneath, allowing extremely long questions to stretch completely across the card without pre-mature wrapping. Build passed and production server restarted on port 3001.
- Restructured AdminPanel layout to support infinite scaling and vertical neatness: implemented client-side pagination (5 items per page) with prev/next navigation buttons for both `คำถามที่กำลังรัน` and `คำถามทั้งหมด` lists, ensuring they never stretch infinitely downwards. Compressed the page floor to a single-column layout centered at max-width 600px, keeping all fixed configurations and paginated lists organized and extremely clean on both desktop and mobile viewports. Build passed and production server restarted on port 3001.
- Implemented full-scale tabbed navigation on AdminPanel to eliminate scrolldown: introduced top horizontal pill-tab controls for admins (`สร้างคำถามใหม่`, `จัดการคำถาม`, `ตั้งค่าหน้าเว็บ & รางวัล`, and `แอดมิน`). Selected areas render exclusively on-screen per tab click, reducing the previous tedious scroll depth to a static, hyper-compact, easy-to-use trading panel. Standardized spacing layouts across all tabs. Build passed and production server restarted on port 3001.
- Customized History modal scrollbars: added webkit scrollbar rules and thin-scroll values to `.modal-body` to apply a vibrant yellow thumb (`#ffe100`) and matching dark track (`var(--card)`) matching the platform's visual tone. Rebuilt and restarted production server on port 3001.
- Separated Tournament Management from Question Creation: added a `tournaments` array to the global `site-settings.json` file. Modified `AdminPanel` to replace the text input for "Tournament" with a select dropdown populated from settings. Added a new "จัดการทัวร์นาเมนต์" (Tournament List) card under the settings tab with functions/inputs to add and delete tournaments cleanly. Build passed and production server restarted on port 3001.
- Implemented Quick Tournament Creator inline in "สร้างคำถามใหม่" tab: added a toggleable `+ เพิ่มทัวร์นาเมนต์ใหม่` button in the Question tab which exposes an inline text input and button; clicking "เพิ่มและเลือก" dynamically saves the tournament via settings API and immediately selects it. Confirmed public front page cleanly hides empty tournaments, showing only those with active open questions. Build passed and production server restarted on port 3001.
- Promoted Tournament Management to a dedicated Tab menu item: added `"tournaments"` to the activeTab state, and added a standalone "จัดการทัวร์นาเมนต์" pill button to the top tab bar. Relocated the entire Tournament List management card (add, delete list) from settings into this exclusive new tab for maximum operational speed. Build passed and production server restarted on port 3001.
- Implemented Advanced Question and Options workflow with Centered Layout in AdminPanel: added `savedQuestions` to settings to store reusable question templates with an inline dropdown selector and a `💾 บันทึกคำถามนี้เก็บไว้` save button; added an `⚡ เปิดทายทันที` quick action setting `opensAt` to current timestamp; created a bulk-line options paste textarea parsing newlines into choices instantly; centered all cards across every tab at `margin: "0 auto"` with `maxWidth: "600px"` / `maxWidth: "760px"`. Build passed and production server restarted on port 3001.
- Repositioned and centered Admin Menu tabs: changed default tab to `"tournaments"` (leftmost), moved the "จัดการทัวร์นาเมนต์" pill tab to the absolute left of the horizontal admin menu, and centered all tab buttons horizontally on screen with `justifyContent: "center"`. Build passed and production server restarted on port 3001.
- Restructured "จัดการคำถาม" tab into distinct operational categories and added permanent deletion: split questions inside the tab into three distinct paginated containers: `คำถามที่กำลังรัน` (active / open / closesAt in future), `คำถามที่หมดเวลา รอคำตอบ` (closesAt in past / closed), and `คำถามทั้งหมดในระบบ` (complete archive). Added a `DELETE /api/admin/predictions/[id]` endpoint cascading deletions of bets safely from `prediction_entries` before deletion of prediction rows; displayed a red `🗑️ ลบคำถามถาวร` button exclusively for `resolved` or `canceled` predictions. Build passed and production server restarted on port 3001.
- Built a secure full-stack "Winner Address Claim" and tracking system: created `data/winner-claims.json` with public `GET/POST /api/claims` endpoints; integrated automated winner detection showing a gold `🎁 Claim Reward` pulsing button and notification banner for the designated winner; built an interactive `ClaimModal` on public front-end for entering shipping details and tracking real-time status; added a dedicated "จัดส่งรางวัล" (claims) tab in the admin panel to review claims and insert tracking numbers. Build passed and production server restarted on port 3001.
- Implemented Admin-Confirmed Winner approval workflow and real-time Esports Analytics Dashboard: added `reward.approved` boolean in settings; public `/api/claims` now returns `null` unless the admin clicks the new `✓ ยืนยัน & อนุมัติผู้ชนะทันที` button in settings; the public gold banner and pulsing gift button persist securely even if closed and can be reopened until shipped; added a standalone `แดชบอร์ด` (Dashboard) tab in the admin panel with a `GET /api/admin/dashboard` API performing multi-table joins on prediction entries, calculating net pool coins, unique user counts, dynamic real-time odds multipliers, and detailing individual bets in a clean paginated card stack. Build passed and production server restarted on port 3001.
- Fixed database relation embed crash and refined Dashboard to display tournament-wide lists: resolved the Supabase join collision error by loading predictions, options, and entries in separate simple queries and matching them in JavaScript. Redesigned the `แดชบอร์ด` UI to let the admin select a Tournament first, showing tournament-wide summaries (total questions, total coins, total unique participants) and rendering all questions under that tournament sequentially on one screen with individual option bars, odds multipliers, and expandible player bet logs. Build passed and production server restarted on port 3001.
- Added Tournament Logo Image Upload and public/admin rendering: updated the `tournaments` settings schema to save object structures `{ name, logoUrl }`; added a file uploader input with real-time base64 image preview in the "จัดการทัวร์นาเมนต์" admin tab; updated public and admin headers to fetch and display the cropped square tournament logo (18x18px) cleanly right in front of the tournament name (falling back to a generic 🏆 icon if no image is uploaded). Build passed and production server restarted on port 3001.
- Implemented complete Month-End Closed-Loop Season finalization: added `season: { startAt, endAt, status }` in settings; added a `POST /api/admin/leaderboard/finalize` endpoint backing up top 20 users' stats into `monthly_leaderboards` and resetting `monthly_profit = 0` for all users dynamically to launch the next season; integrated a real-time countdown to `season.endAt` with UTC+0 timezone rendering; public frontend now blocks bets and displays a clean "Season Has Ended" alert when the season is inactive. Build passed and production server restarted on port 3001.
- Fixed Season Date saving and countdown update: Added missing `season` field to `SiteSettings` type definition and the initial default template in the backend `app/api/admin/settings/route.ts` API. Updated the `PATCH` handler to parse and persist the `season` object in `site-settings.json`, resolving the bug where season end dates remained stuck on May 31, 2026 17:00 UTC+0. Identified and resolved Next.js static page cache build-time optimization: added `export const dynamic = "force-dynamic"` to `/api/settings` and `/api/predictions/open` route handlers to force real-time reading from files and database on every request, completely clearing stale frontend cached dates and data. Successfully ran `npm run build` and restarted the Next.js production server on port 3001.
- Restored simple, manual, and explicit controls for Season End and Winner claims: Discarded full black-box automation in favor of three transparent, independent steps in the Settings tab: 1) Season Dates management with manual datetime-local inputs allowing manual addition/subtraction/extension of days; 2) Reward & Winner declarations selecting which Month/Year, naming the reward, choosing the top winner, and toggle-switching "เปิดเคลมหน้าแรก" explicitly; 3) Monthly Leaderboard reset which ONLY archives Top 20 snapshots and clears active profit to 0. Restructured backend endpoints and Settings tab interface to perfectly sync these three tasks. Build passed and server restarted on port 3001.
- Consolidated settings tab into exactly 2 simplified manual steps: Merged "Season Dates" and "Winner & Reward Setup" into a single intuitive card ("1. ตั้งค่ารอบกิจกรรม & ของรางวัลประจำซีซั่น") with a single "บันทึกตั้งค่ากิจกรรม & รางวัล" button. Isolated the final standings rollover as step 2 ("2. เคลียร์คะแนนกระดานเพื่อเริ่มเดือนใหม่") triggering database resets purely. This guarantees complete explicit manual control, eliminates step overhead, and makes the flow completely basic and straightforward. Build compiled successfully and server restarted on port 3001.
- Simplified winner claims tracking and status UI: Changed status badge options for tracking in the Admin Claims tab to show "กำลังจัดส่ง" in green once shipped. Renamed the action button to "แจ้งสถานะกำลังจัดส่ง" and streamlined input variables to capture the tracking ID and lock progress with maximum clarity and speed. Build succeeded and server restarted on port 3001.
- Fixed stale front-end countdown and fully removed manual tracking fields: Resolved state synchronization issue where the homepage countdown did not update immediately upon settings save by adding `settings` to the `useEffect` timer dependency array in `SuperWinPrototype.tsx`. Completely stripped the tracking ID input field from the Admin claims tab and updated the public claims modal so that clicking "แจ้งสถานะกำลังจัดส่ง" sets status to "completed" and displays a clean, elegant shipment tracking notification without requiring any typed input. Build passed and production server restarted on port 3001.
- Implemented real-database Leaderboard synchronization for Monthly Top 10: Created a public GET `/api/leaderboard` API endpoint fetching real player rankings based on `monthly_profit` descending from Supabase (excluding admins) and falling back gracefully with 0-profit placeholder records. Wired `SuperWinPrototype.tsx` to dynamically fetch this API on mount and sync the user's real scores, ensuring that clicking "Reset Standings" in the admin panel instantly reflects 0 points for everyone on the public home-screen leaderboard. Build succeeded and server restarted on port 3001.
- Resolved server-side infinite loop hang on public leaderboard endpoint: Identified and resolved an infinite `while` loop condition inside `app/api/leaderboard/route.ts` where fewer than 10 database users caused a loop forever due to a 9-item mockNames array. Expanded the mock names array to exactly 10 items ("Storm" added) and integrated a loop index breakout guard (`i < 100`), ensuring the website loads and displays instantly with 100% stable performance. Build succeeded and production server restarted on port 3001.
- Implemented real-time periodic data synchronization on the public home page: Added a background sync interval hook executing every 10 seconds inside `SuperWinPrototype.tsx` to automatically re-fetch the logged-in player's balance and profit (/api/me) alongside the global standings (/api/leaderboard) in the background. Payouts and scoreboard changes now propagate instantly onto the user's open tab without requiring manual browser page refreshes (F5). Build passed and server restarted on port 3001.
- Streamlined claims to "Delivered Successfully" status: Shifted status representation in the Admin Claims tab from "กำลังจัดส่ง" (shipping) to "จัดส่งสำเร็จ" (delivered successfully) on a single click. Changed the action button label to "✔️ แจ้งว่าจัดส่งสำเร็จ" and updated the frontend `SuperWinPrototype.tsx` home banner and claim modals to display "Prize delivered!" and "Status: Delivered Successfully" cleanly, eliminating any reference to intermediate shipping stages or tracking requirements. Build compiled successfully and server restarted on port 3001.












