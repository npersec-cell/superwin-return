# 2026-05-30

- Implemented an auto-disappearing mechanism for admin messages:
  - Added a React hook (`useEffect`) in `components/AdminPanel.tsx` that monitors the `message` state.
  - Automatically clears the banner notification (e.g. "บันทึกข้อความ Info สำเร็จ") 4 seconds after it is set, preventing it from permanently occupying space on the screen and improving the overall user experience.
  - Verified and compiled Next.js build successfully.
