-- Optional manual seed for SUPERWIN RETURN demo predictions.
-- The app also includes scripts/seed-predictions.mjs for automated local seeding.

-- If you use this SQL manually, insert predictions first, then use returned ids
-- to insert rows into prediction_options. The Node seed script is safer because
-- it avoids duplicates by checking existing question/option labels.
